# Maintainer information

Software: `anaconda-channel-guide`.

## Versioning scheme

`anaconda-channel-guide` uses the
[Python versioning scheme (PEP 440)](https://packaging.python.org/en/latest/specifications/version-specifiers/#version-scheme).
Final releases use increasing `MAJOR.MINOR.PATCH` version numbers, such as `0.2.0`.
Version comparison and ordering follow PEP 440.

Release tags have a `v` prefix, such as `v0.2.0`. The build uses `hatch-vcs` to
derive the package version from Git tags.

## Security contact

For vulnerability reporting and Anaconda's coordinated vulnerability disclosure
policy, see [Anaconda's security contact information](https://www.anaconda.com/.well-known/security.txt).
